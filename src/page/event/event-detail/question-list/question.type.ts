export interface QuestionProps {
  prefix?: string;
}

export interface FilterProps {
  prefix?: string;
}
export const PREFIX_QUESTION = 'question';
export const FILTER_KEY = {
  KEYWORD: 'keyword',
  STATUS: 'status',
};
